import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.codingtechnyks.gps',
  appName: 'geolocation-enable-gps',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
